apiVersion: v1
kind: Service
metadata:
  name: {{ include "bot_chart.fullname" . }}
  labels:
    {{- include "bot_chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bot_chart.selectorLabels" . | nindent 4 }}
